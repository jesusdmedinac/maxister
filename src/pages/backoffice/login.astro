---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Backoffice Acceso Root — Maxister">
  <div class="flex min-h-screen items-center justify-center bg-[#141414] px-4 font-sans text-[#ececec]">
    <div class="w-full max-w-sm rounded-3xl border border-white/10 bg-[#1c1c1c] p-7 shadow-2xl">
      <div class="mb-6 text-center">
        <div class="mx-auto mb-3 flex size-12 items-center justify-center rounded-2xl bg-paradiso/20 border border-paradiso/30 text-paradiso-300 text-xl font-bold shadow-lg shadow-paradiso/10">
          🏛️
        </div>
        <h1 class="text-xl font-bold text-white tracking-tight">Backoffice Root Admin</h1>
        <p class="mt-1 text-xs text-white/50">
          Acceso exclusivo para administración central de profesores y memoria estratégica
        </p>
      </div>

      <form id="login-form" class="space-y-4">
        <div id="error-msg" class="hidden rounded-xl border border-red-500/20 bg-red-500/10 p-3 text-xs text-red-400"></div>

        <div>
          <label class="block text-xs font-medium text-white/70 mb-1.5">Correo de Administrador</label>
          <input
            type="email"
            id="email"
            required
            placeholder="admin@desde0.dev"
            class="w-full rounded-xl border border-white/10 bg-[#121212] px-3.5 py-2.5 text-xs text-white placeholder-white/30 outline-none focus:border-paradiso focus:ring-1 focus:ring-paradiso transition"
          />
        </div>

        <div>
          <label class="block text-xs font-medium text-white/70 mb-1.5">Contraseña Root</label>
          <input
            type="password"
            id="password"
            required
            placeholder="••••••••••••"
            class="w-full rounded-xl border border-white/10 bg-[#121212] px-3.5 py-2.5 text-xs text-white placeholder-white/30 outline-none focus:border-paradiso focus:ring-1 focus:ring-paradiso transition"
          />
        </div>

        <button
          type="submit"
          id="submit-btn"
          class="w-full rounded-xl bg-paradiso py-2.5 text-xs font-bold text-white shadow-lg shadow-paradiso/20 hover:bg-paradiso-600 active:scale-[0.98] transition cursor-pointer"
        >
          Ingresar al Backoffice
        </button>
      </form>

      <div class="mt-6 text-center text-[11px] text-white/40">
        <a href="/" class="hover:text-paradiso-300 transition">← Volver al chat de Maxister</a>
      </div>
    </div>
  </div>

  <script>
    const form = document.getElementById('login-form');
    const errorMsg = document.getElementById('error-msg');
    const submitBtn = document.getElementById('submit-btn');

    form?.addEventListener('submit', async (e) => {
      e.preventDefault();
      errorMsg?.classList.add('hidden');
      if (submitBtn) submitBtn.setAttribute('disabled', 'true');

      const email = (document.getElementById('email') as HTMLInputElement)?.value;
      const password = (document.getElementById('password') as HTMLInputElement)?.value;

      try {
        const res = await fetch('/api/backoffice/login', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ email, password }),
        });

        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Credenciales inválidas');

        window.location.href = '/backoffice';
      } catch (err: any) {
        if (errorMsg) {
          errorMsg.textContent = err.message || 'Error de acceso';
          errorMsg.classList.remove('hidden');
        }
      } finally {
        if (submitBtn) submitBtn.removeAttribute('disabled');
      }
    });
  </script>
</Layout>
